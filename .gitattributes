# Tell the export process to ignore the .gitattributes and .gitignore files
.gitattributes export-ignore
.gitignore export-ignore

# Set default behavior, in case users don't have core.autocrlf set.
* text=auto

# Declare files that will always have CRLF line endings on checkout.
*.txt text eol=crlf
*.log text eol=crlf

# Declare file extension languages that aren't obvious
*.tsp linguist-language=Tcl

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.pdf binary
*.gif binary
*.ico binary
*.exe binary
*.dll binary
*.mnu binary
*.chm binary
*.gnu binary
*.xls binary
*.bat binary
*.gpl binary


